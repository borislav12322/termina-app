<!DOCTYPE html>
<html>
<head>
    <style>
        @media print {

        }
        @page {
            margin: 0;
        }
        html, body {
            margin: 0;
            padding: 0;
            width: 80mm;
            height: 80mm;
        }
        .page {
            width: 80mm;
            height: 80mm;
        }
        .page img {
            max-width: 100%;
            max-height: 100%;
            /*transform: rotate(90deg);*/
        }
    </style>
</head>
<body>
    <div class="page">
        <img src="<%= image %>" />
    </div>
</body>
</html>
