const PublicRoute = ({children, restricted}) => {
    return
    // if (getToken() && restricted) {
    //     return <Navigate to={`${paths.visitors}`} state={{from: location}}/>;
    // } else {
    //     return <>{children}</>;
    // }
};

export default PublicRoute;