import React from 'react';

const PrivateRoute = ({
                          children,
                          scopesForSuccess,
                      }) => {
    return
    // if (getToken()) {
    //     return <>{children}</>;
    // } else {
    //     return <Navigate to={`/login`}/>;
    // }
};

export default PrivateRoute;